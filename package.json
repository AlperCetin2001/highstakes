{
  "name": "high-stakes-server",
  "version": "1.0.0",
  "description": "High Stakes Multiplayer Server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "colyseus": "^0.15.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "@colyseus/schema": "^2.0.0",
    "@colyseus/monitor": "^0.15.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
